@id("Allow users with Admin role to view all UI components")
permit (
  principal,
  action == AgamaLab::Action::"View",
  resource is AgamaLab::UIComponent
)
when {
  principal has id_token && 
  principal.id_token has username && 
  principal.id_token.username == "gasmyrmougang@yahoo.fr"
};